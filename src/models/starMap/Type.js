class Type {
    /**
     * Constructeur de type
     * @param {string} typeCode
     * @param {string} nom
     */
    constructor(typeCode,nom) {
        this.typeCode = typeCode
        this.nom = nom
    }
}